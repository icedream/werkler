version: "2"
run:
  timeout: 5m
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - usetesting
  settings:
    usetesting:
      context-background: true
      context-todo: true
formatters:
  enable:
    - gofmt
